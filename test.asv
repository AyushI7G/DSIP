x = [1 2 0.5 1];
h = [1 2 1 -1];

h_fold = fliplr(h);
h_shift = circshift(h_fold,-1);

figure;

subplot(3,3,1);
stem(k,x);
title('x[k]');
xlim([-3,3]);

subplot(3,3,2);
stem(k,h_shift);
title('h[-k] n = -1');

subplot(3,3,3);
stem(k,x.*h_shift);
title('Multiplication');


